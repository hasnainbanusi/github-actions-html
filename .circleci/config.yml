version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout

      - run:
          name: Check HTML
          command: |
            test -f index.html
            echo "HTML file exists successfully"
            cat index.html

workflows:
  html-workflow:
    jobs:
      - build
